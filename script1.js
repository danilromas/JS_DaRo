"use strict";
for (let i = 10; i <= 1000; i++) {
	res = String(i);
	console.log(res1[0]);
 };
 
 
 let res = 0;
 for (let i = 10; i <= 1000; i++) {
	str = String(i);
	res = Number(str[0]) + Number(str[1])
	console.log(res);
 };
 
 for (let i = 10; i <= 1000; i++) {
	str = String(i);
	if (str[0] == 1) {
	   console.log(Number(str));
	} else {
	   continue;
	};
 };