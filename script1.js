"use strict";
let num = 1, i = 1;

for (i; ; i++) {
   if (num + i > 100) {
      break;
   if (elem % 2 == 0) {
	result = elem * elem;
 } else if (elem % 3 == 0) {
	result = elem * elem * elem;
} else
	{continue;
}

console.log(result);
	}
}