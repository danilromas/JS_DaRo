"use strict";
let newArr = [];
for (let i = 0; i <= 2; i++) {
   newArr[i] = [];
   for (let k = 1; k <= 5; k++) {
      newArr[i].push(k)
   };
};
console.log(newArr);
let newArr = [];
for (let i = 0; i <= 2; i++) {
   newArr[i] = [];
   for (let k = 1; k <= 4; k++) {
      newArr[i].push('x')
   };
};
console.log(newArr);